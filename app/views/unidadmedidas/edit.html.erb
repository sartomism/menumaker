<h1>Editing unidadmedida</h1>

<% form_for(@unidadmedida) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @unidadmedida %> |
<%= link_to 'Back', unidadmedidas_path %>
